"""
This page is reserved for an algorithm that will create an all-new type at runtime,
with megabyte to not be restricted at all.

data => meta learner model that learns type => generates new type and holds it in cache


# Ideas
- Meta learners like Koi, with evolutionary algos
- Terrabyte but really small, nn that learns type and creates byte stream of new type
"""
